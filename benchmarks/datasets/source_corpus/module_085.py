# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_0(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_0(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_1(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_1(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_2(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_2(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_3(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_3(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_4(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_4(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_5(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_5(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_6(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_6(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_7(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_7(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_8(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_8(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()

# @ecdat-synthetic-corpus
from cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes
from cryptography.hazmat.backends import default_backend

def init_cipher_85_9(key, iv):
    algo = algorithms.AES(key)
    mode = modes.GCM(iv)
    return Cipher(algo, mode, backend=default_backend()).encryptor()

def hash_data_85_9(data):
    import hashlib
    return hashlib.sha256(data).hexdigest()
