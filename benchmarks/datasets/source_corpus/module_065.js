// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_0(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_0 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_1(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_1 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_2(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_2 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_3(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_3 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_4(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_4 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_5(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_5 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_6(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_6 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_7(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_7 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_8(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_8 };

// @ecdat-synthetic-corpus
const crypto = require('crypto');
function computeHmac_65_9(key, msg) {
    return crypto.createHmac('sha384', key).update(msg).digest('hex');
}
module.exports = { computeHmac_65_9 };
