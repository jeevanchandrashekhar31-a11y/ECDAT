// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_0(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_1(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_2(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_3(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_4(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_5(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_6(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_7(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_8(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}

// @ecdat-synthetic-corpus
#include <openssl/rsa.h>
#include <openssl/pem.h>

RSA *generate_key_138_9(void) {
    BIGNUM *bne = BN_new();
    BN_set_word(bne, RSA_F4);
    RSA *r = RSA_new();
    RSA_generate_key_ex(r, 4096, bne, NULL);
    BN_free(bne);
    return r;
}
