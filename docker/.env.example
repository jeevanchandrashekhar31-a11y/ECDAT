# Local demo values only. Copy to repository-root .env before starting Docker.
POSTGRES_DB=ecdat
POSTGRES_USER=ecdat_app
POSTGRES_PASSWORD=change-this-local-postgres-password
ECDAT_API_KEY=change-this-local-api-key
FRONTEND_PORT=8080
BACKEND_PORT=5000
