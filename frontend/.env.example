# ECDAT Frontend Environment Variables

# Backend API Base URL (leave empty if using Vite proxy / reverse proxy in production)
VITE_API_BASE_URL=

# Default application port when running locally with Vite
PORT=3000
